<html>
<head>
    <title>{{ title }} Page</title>
</head>
<body>
    <h1>{{ title }}</h1>
    <h3>Name: {{ user.Name }}</h3>
    <h3>Age: {{ user.Age }}</h3>
    <h3>Birth: {{ user.Birth }}</h3>

    <div>powered by <strong>{{ engine }}</strong> view engine</div>
</body>
</html>